# Cumulative research backlog

Status: NO_EDGE_VALIDATED. Research artifacts only; no production changes or execution.

Current automation state: **PAUSED**, verified September 21. Do not resume it or start a persistent collector.

## Completed September 21 deep dive

- Investigated 12 outcome-independent development launches and 12 later verified launches. Development first-twelve prefix established; later selection remains partial because two earlier creation records are unresolved. All development activity intervals exhausted; 11/12 later intervals exhausted, with `.family` capped and VRX/JABAL decoding limitations retained.
- Frozen eight anonymous wallets by early-entry recurrence, not returns. Two wallets recur across development tokens; `JCZXR5AQyxyerEWWLWCqefen3otCTgEx3W9sHm1LThcs` also appears in the later sample. Its later **34%** buy at token age 8s and same-amount token disposal at 12s precede all configured follower submission times. This is a timing result, not profit or validated alpha.
- Implemented frozen trailing H1/H2/H3 triggers, momentum and deterministic ordinary-buyer matching. Observed development trigger counts: 7/3/1, momentum 3; later counts: 1/0/0, momentum 4. September execution regimes fail compatibility: no simulated returns or fabricated slippage rejections. Related-wallet coverage is bounded, not proof of independence.
- Completed September 10–12 GMannnnn coverage (78 records) and a diagnosed direct-address Unipcs subset (177 records). Unipcs all-token-account capture remains partial at 4,000 records. Preserve buy/sell-like versus independently verified-fill distinctions; receipts and sponsored network fees are not follower PnL.
- Four additional default-feed profiles retained: ballerboi, Onchainmetrics, NBA__trey, menalwaysregret. Zero new mappings admitted. Explicit linked-X evidence inspected without guessing social identities.
- Three primary papers reviewed; pinned September Pump IDL archived. Independent balance audit corrected absent token balances from zero to unknown, with regression coverage. Corrected audit: 1,331 matching observed native/base pairs, seven missing-evidence pairs.
- **108 tests pass; zero-request offline reproduction passes.** 94 requests, 28,237 unique captured signatures. Successful-response credit estimate **3,720**; conservative reservations **6,970/10,000**, actual billing unobserved. Prior closed-session artifacts and separate September 13 settings preserved. Core holdout lock intact; post-lock coverage-only driver explicitly disclosed, so do not reuse this later sample as untouched validation.
- Report: `2026-09-21-deep-dive-report.md`. Reproduce: `python3 docs/research/memecoin/deep_report.py --verify-only`. No automatic continuation, collector, signal or trade enabled.

## Completed September 21 continuation

- Recovered the single truncated winning-path event from cached Pump self-CPI instruction data, with the expected authority/layout, actual reserve deltas, complete adjacent coverage and independent checks on both neighbors. Recovery is opt-in and signature-scoped; the default strict decoder remains unchanged. **193 fills reproduce; 191/191 adjacent checks pass.** Zero new RPC calls for recovery.
- Re-ran the frozen calibration matrix: still six conditional simulations and twelve entry-slippage rejections. Winning exit-state coverage is now supported, but its rejected entries remain rejected. Primary loss stays −0.045596413 SOL; no profitable strategy inference.
- Completed both existing CATE minute windows: **1,231 unique records, 389 failed**. New seconds contain two fully reconciled GMannnnn fills from `FPLXEqws2rKy4k8B2tARTGV632fYCnPqxEktVMKfJ2H9`; each is the unique direct-input amount match in its complete window. Independent visible relative/absolute timestamps versus UTC corroborate the frozen UTC−3 offset at minute precision. Mapping admitted under the two-fill rule; a third exact amount reconciliation has truncated logs and is supporting only. No insider/performance claim or retroactive identity availability.
- Completed the frozen five-minute Unipcs wallet/token-account window: **119 records = one verified buy, 111 incoming transfers, one outgoing transfer, five related-account failures, one no-change record**. Other-payer network fees are not charged to this wallet. The known buy's 50,000-USDC debit already includes its 22.50 fee-like transfers. Observed transient WSOL closure and reimbursement reconcile the 2,039,280-lamport parser residual without inventing a future refund.
- Frozen next chronological experiment for **September 13 UTC**, the first full day after the source wallet's latest baseline cache record. First 20 qualifying buys; development first ten, holdout next ten. No outcomes acquired for this experiment; known calibration mints excluded, no replacement for poor/unsupported execution.
- **75 tests pass**. Explicit session configuration, universal closure/deadline guards, cross-session cache reuse and durable reservations implemented. Original raw captures and closed-session artifacts preserved. New Helius successful-response estimate **160 credits**, conservative reservations **180/1000**; actual billing unobserved. Two sandbox DNS failures received explicitly audited one-time retries outside the sandbox; no automatic retry loop.
- Report: `2026-09-21-continuation-report.md`. Reproduce offline: `python3 docs/research/memecoin/continuation_report.py --verify-only`. Automation stays paused; execution disabled.

## Completed September 21

- Implemented research-only capture/decode/integer quote/replay/discovery interfaces. Frozen experiment in `session_20260921_manifest.json`; report in `2026-09-21-follower-replay-pilot.md`. Existing caches preserved, branch `feat/cryptofutures_v3` unchanged.
- Resolved the ordinary-versus-exact-input discrepancy using the **pre-trade May 18 IDL's documented formula**, not a fitted constant. Historical event layout is explicitly checked. All 192 admitted fills reproduce; 189/190 independent adjacent state/quote checks pass. The losing path passes 81/81. The one winning-path gap is retained, not patched over.
- Verified the observed fee regime: 95bps protocol plus 30bps redirected creator cashback; 5000 means half the protocol fee is redistributed to buyback, not an additive 50% trade fee. Protocol/buyback balance changes match 192/192. Cashback differences identify five creation deposits and seven claim/closure transactions; hypothetical refunds are not assumed.
- Completed bounded fixed-exit acquisition and same-slot ordering for the frozen native-SOL curve pilot. Eighteen sensitivity rows: six conditional simulations, twelve entry-slippage rejections. Primary losing case: 0.1 SOL, 15s delay, fixed +90s exit gives −0.045596413 SOL after median observed costs, excluding the separately locked deposit. Winning-case entries all reject; their later exit coverage remains unsupported because of truncated logs. These are known-outcome calibration cases, not a validated strategy or eighteen independent observations.
- Completed a frozen convenience panel of three additional public Fomo profiles and explicit linked-X checks where available. **Zero new wallet mappings admitted.** Preserve Rochaboyyyy, therear66 and GMannnnn, including unsupported-chain, missing-identity, and partial-history failures; do not replace them with profitable profiles. See `session_20260921_candidates.json`.
- Added 115 unique calibration-market records and 200 bounded discovery records. Conservative Helius reservation: **262/1000 credits**; successful-response estimate **242**, provider billing unobserved. Two failed version-0 discovery captures remain reserved; one bounded corrected version-1 pass succeeded. No retry loop, upgrade or funding.
- Offline reproduction: `pilot_decode.py`, `pilot_replay.py`, `pilot_discovery.py`; tests via `python3 -m unittest discover -s docs/research/memecoin -p 'test_*.py'`. New requests are guarded by the frozen session deadline. No trades/signals/background collection enabled.

## Completed September 16

- Decode the original 200 cached transactions into conservative flow ledgers.
- Match August 30 public Fomo USELESS buy to owner-signed swap and exact USDC reconciliation.
- Independently match August 25 public Fomo USELESS buy: signature `sspQjKk7ZVutys2gHMF76VW7VQ4uD4nmeN2hahGtHPw7iuRMErugL7KsDeGDc1ZduAPRZXatP4q8236hZwMLq2S`; timestamp 2026-08-25 20:45:04 UTC, matching 17:45 UI at inferred UTC−3. Owner debited 17,675.44 USDC, swap transfer 17,666.60228 USDC, separate transfer 8.83772 USDC; received 284,217.750173 USELESS. Successful routed swap logs, owner signer. Public displayed buy $17,666.60 matches rounding. Separate recipient's fee role remains inferred, not established.
- Second targeted query returned 15 rows; estimated 10 new credits. Cache `unipcs_useless_aug25.json`; no pagination. Two distinct Fomo events now strongly corroborate this profile-wallet mapping, not profitability.
- Inspect the first historical single-buy/single-sell pair at instruction level: mint `435CfmvrJ4QtkVv4cZ1TbUa7rzrKKjvQJLMnvhTCpump`. Successful Buy and Sell logs, token amount round trip, 61 seconds between them. Buy native cashflow -1.505079080 SOL; successful sell +1.472218686 SOL; additional failed sell -0.002611594 SOL. Total observed cashflow -0.035471988 SOL. Buy account creation locks 0.002074080 SOL; excluding that deposit gives -0.033397908 SOL. This is a losing example, not evidence of cohort-wide profitability or a complete portfolio return. Native deltas already include transaction fees: never subtract them twice.
- Automation prompt updated to resume this backlog and execute successive experiments, instead of stopping after proposing one. Existing finite three-times-daily schedule retained; not continuous capture.
- Ran `audit_observed_cycles.py` across the entire cached sample. All twelve balanced-mint groups have Buy logs on inflows and Sell logs on outflows; six show positive and six negative observed native cashflow. Combined observed cashflow is +3.015797035 SOL, including assigned failed attempts, but excluding 27 unassigned records and the incomplete-inventory mint. Group spans range from 41 to 869 seconds (not necessarily continuous holding periods). These are preliminary cashflow groups, not a validated win rate, complete wallet PnL, or follower returns. Full instruction/rent/counterparty adjudication remains unfinished beyond the first pair.
- Added and passed three offline regression tests covering both Fomo matches, signer versus payer, nonmutation, failed classification, receipt exclusion and the losing pair's failed-attempt cost. Run `python3 -m unittest discover -s docs/research/memecoin -p test_cached_evidence.py`.

## Active work queue: continue in order

September 18 afternoon checkpoint: resolved the one net-token mismatch as an exact-amount incoming-then-outgoing routed sell. Extended event tails and completed integer-formula/base-fee checks on all 102 trades: 89 ordinary buys/sells reproduce exactly; 13 exact-input buys differ by one lamport under the ordinary buy formula. All base fees match ceiling rounding. Tail cashback/buyback denominator semantics remain unverified; do not treat 5000 as an additive 50% trade fee. See `2026-09-18-rounding-and-routing.md`. Next: primary-source fee semantics, exact-input implementation, and independent pre-state continuity—not more fitted arithmetic. Zero new Helius credits.

September 18 checkpoint: recovered 102 historical curve-state events from cached logs using published TradeEvent prefix. All timestamps match, 101/102 user token deltas match. Exact constant offset of 206900000000000 raw tokens between account balance and event real reserve is recorded, not assumed spendable. Virtual reserves and prefix fees are available; fee tails remain unparsed. See `2026-09-18-historical-state-events.md`. Next executable prerequisite: validate full historical fee layout and reproduce actual trade amounts from reserves/rounding; then request missing fixed-exit windows. Zero new Helius credits.

September 17 evening checkpoint: acquired and analyzed both winning-case entry windows (34 curve records, 41 AMM records; estimated 20 new Helius credits). Known buys present in both. Ratio changes at +5s are +6.58% curve and +1.55% AMM; +15s +20.24–21.06% and +10.63%; first observations at/after +30s +31.74% (actual +32s) and +15.03%. Diagnostic realized exchange ratios only, not follower returns. Completed the requested winner/loser entry comparison and documented counterfactual specification v0 in `2026-09-17-winning-entry-probes.md`. Next priority: historical state and fixed-exit coverage needed to make a size-aware simulation runnable; stop collecting entry-only anecdotes. No validated edge.

September 17 second checkpoint: completed transaction-boundary deposit audit (nine transient/zero accounts, twelve surviving owner token accounts) and progressed directly to market-path acquisition. One bounded Helius request, estimated 10 credits, captured 100 records around the losing cycle: 69 successful reserve flows, 31 failures; final 21 seconds of requested window missing. Observed reserve-ratio changes at +5/+15/+30 seconds are +0.71%/+3.11%/+11.66–14.79%, not executable follower prices. See `2026-09-17-first-delay-probes.md`. Next: comparable winning-cycle market capture and explicit counterfactual execution assumptions; do not repeat completed wallet/reserve audits. Later rent refunds and unknown program receipts remain unresolved accounting exclusions.

September 17 checkpoint: completed reserve-pair verification for all 55 successful owner-token flows, with exact opposing token quantities and quote direction. 49 use native-SOL curve reserves; six require WSOL AMM reserve balances. All twelve balanced groups have observed zero opening and closing owner tokens. The missing-acquisition mint has a confirmed nonzero opening balance. Six regression tests pass. See `2026-09-17-reserve-verification.md`; zero new Helius credits. Next unresolved accounting item is retained versus transient rent, not redoing token-flow or reserve matching.

September 16 evening checkpoint: completed `reconcile_native_cashflows.py` over all 100 records and inspected the 27 previously unassigned records. 55 total / 17 unassigned reconcile from parsed system movements and fees; all 18 failures reconcile to fees alone. Remaining residuals include direct program lamport changes and account closures. See `2026-09-16-cashflow-reconciliation.md`. Completed machine-readable two-fill adjudications and expanded tests (five passing). Concentration experiment: removing the largest cashflow group reduces +3.015797035 SOL to +0.180526507 SOL; removing the two largest gives -0.758543349 SOL. Not a full PnL or edge estimate. No new Helius credits.

1. **Completed:** signature-scoped embedded-event recovery, regression tests and independent adjacent-state validation. Do not re-fetch the known truncated archive logs or globally accept truncated records. See continuation report.
2. **Next execution dependency:** validate one explicitly selected September non-Mayhem native-curve fee/reserve regime using historical configuration, both quote directions and independent account state. Then freeze a new untouched chronological window for the existing wallet list and controls; do not tune on the inspected September 17 sample. Mayhem, holder rewards, AMM and migration execution remain unsupported. The separate `data/session-2026-09-21-continuation/next_experiment.json` still fixes September 13 UTC and first 20 source buys, untouched and not superseded; it needs its own bounded acquisition session. No retuning to rescue calibration losses.
3. **Wallet accounting coverage completed where stated, not profitability:** GMannnnn September 10–12 history and Unipcs direct-address subset are now complete; broader Unipcs token-account history and general AMM buy/sell adjudication remain partial. Four new social-profile failures are preserved alongside Rochaboyyyy/therear66. Before collecting more usernames, use the recurring-wallet timing evidence to specify a separate persistence/latency observation test; any new filter needs a fresh frozen experiment, not post-hoc rescue.
4. Reserve matching and observed opening/closing inventory checks cover all original groups. Legacy full-wallet accounting remains partial: 10/27 previously unassigned records still have system-transfer-parser residuals. Gross account creation is not retained rent; extend direct receipt/closure reconciliation where needed. Do not label partial cashflows as complete portfolio PnL or a win rate.
5. Prospective paper observation and live source-to-bot latency measurement require a separate capture/deployment decision. Include failed/stagnant tokens. Keep scheduled research paused; no new jobs, trades, alerts or production changes under this backlog.

## Completion rule for each run

Resume the first unfinished actionable item. Save reproducible evidence, update completed items, and move to the next safe item rather than stopping at another plan. A three-day pilot can validate data plumbing and reject hypotheses; it cannot guarantee a durable trading edge. Keep production trading disabled. Notify for substantive results, not routine backlog bookkeeping.
