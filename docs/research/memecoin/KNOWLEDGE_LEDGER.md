# Memecoin knowledge ledger

Status: current summary, 2026-09-24. Historical source paths are on #27 (`d01d865`) and #30 (`9804d6c`); see [HISTORY.md](HISTORY.md). These are observations, not inherited next-step instructions.

| ID / status | What evidence says and why | What it does not say | Reopen condition |
| --- | --- | --- | --- |
| K1 `KNOWN` | #27's frozen follower pilot had six conditional modeled losses on one losing fixture; twelve other size/delay settings rejected entry. The winning fixture rejected all nine entries at frozen 2% slippage. The integer curve replay and cashflow checks support those conditional mechanics. | It does not estimate general follower expectancy or prove wallet information useless. Two selected outcomes are dependent fixtures, and live latency was unmeasured. | New independent chronological cohort with complete negative universe, available-at clocks, matched controls and costed fills. |
| K2 `KNOWN` | #27 found token receipts that were transfers, not buys; one verified 50,000-USDC buy required signer and cashflow reconciliation. | Incoming tokens alone cannot establish wallet intent, skill or profitability. | New transaction-level accounting with independent wallet attribution. |
| K3 `KNOWN` | #30's 19/19 checked largest token accounts matched PumpSwap pool base vaults. | Raw `top1_holder_bps` is not developer or whale concentration. | Holder account semantic classification on an independent cohort or protocol layout change. |
| K4 `KNOWN` | #30 held-out check found both earlier `WATCH_ENTER` rows below its liquidity floor about 3h 17m later. Later vault reads also disagreed with the strict Dex quote-side cent-match claim on 7/19. | Vault changes are not trade returns, fills or proof every radar row fails. Later reads cannot relabel earlier CLI decisions. | Prospectively timed complete observations and executable quotes. |
| K5 `KNOWN` | `main` offers raw fsynced spool and caller-supplied PIT scan/evaluate primitives, with `NO_EDGE_VALIDATED`. | This is not a live early-launch collector or validated strategy. | Instrument a measured, covered prospective event stream. |
| K6 `OPEN` | Participant/creator history may contain prospective information. #27 motivates careful identity and cashflow semantics. | No incremental value over simple early-state baselines is established. | Freeze sample-size-aware features, negative universe and temporal ablation. |
| K7 `REJECTED` | Treating high graduation probability as a direct entry rule conflates lifecycle classification with costed trade expectancy. | Graduation labels can still help survival modeling. | Only a separately validated costed economic mechanism can support an entry rule. |
| K8 `REJECTED` | Tuning #30's $10,000 floor to manufacture `WATCH_ENTER` rows addresses label frequency, not outcome validity. | The old radar can remain historical intake evidence. | A new, predeclared economic experiment with independently timed data. |
| K9 `KNOWN` | MC-001's short prospective native log capture matched 2,458/2,458 independent signatures in interior slots and decoded six creates/437 trades; 113/113 new native non-Mayhem state transitions reconciled. Raw bytes, index pages and pinned IDL are preserved. | It does not prove 24/7 stream coverage, precise network latency, enough outcomes or an execution edge. | Replicate in longer/different windows or change provider/protocol version. |
| K10 `KNOWN` | MC-002's 180-second capture matched 37,557/37,557 independent interior signatures; 4,682/4,682 supported new-mint transitions reconciled. Of 104 creates, only 19 had the frozen costed quote outcome; 12 exits lacked sellable liquidity and 10 entries lacked an as-of fee state. | The three quoted evaluation returns cannot establish curve-progress alpha; unavailable exits cannot be omitted to imply profitability. | Independent longer replication and explicit exit-risk outcome as frozen in MC-003. |
| K11 `KNOWN` | A second RPC host independently matched all 37,557 MC-002 interior signatures. MC-003 matched 38,941/38,941 on that second host; its frozen rank had negative mean quote returns in development and evaluation. Twelve of 52 eligible MC-003 launches lacked a sellable exit quote. | Reject the unchanged five-second curve-progress rank as an entry basis under the tested cost proxy. Keep exit failure separate from quoted returns and report explicit zero-recovery stress. | Longer prospective coverage, transaction balance deltas, observed inclusion and exit-route feasibility, then prespecified survival/risk features on fresh windows. |
| K12 `KNOWN` | MC-004 fetched 56/56 hash-selected MC-003 transactions. Single-event token deltas reconciled in 47/48; one create-and-buy had no curve pre-balance. Fifteen of 48 single-event whole-transaction fees exceeded 155,000 lamports. | The old flat network-cost scenario is not uniformly conservative; fee-payer net balance change includes trade/rent/router movements. No sample fee is an achieved hypothetical Rocket fill. | Predeclare fee scenarios from observed distributions, then measure prospective inclusion, failures and route-specific exits. |
| K13 `KNOWN` | In MC-003, eleven of twelve `EXIT_UNAVAILABLE` last curve states had only 1–18 lamports of real SOL reserve; the twelfth had 2,074,072 lamports. | The modeled Pump curve route was depleted for the stressed sell. This does not rule out an alternate venue or prove realized zero recovery. | Identify migration and alternate exit-route state with transaction-level observations. |
| K14 `KNOWN` | MC-005 matched 152,120/152,120 interior signatures. The frozen net-flow top group had lower modeled curve-exit unavailability in development and evaluation. Its held-out positive mean return disappeared when one +288.65% modeled winner was removed; the development top group was negative. | Early flow is a candidate risk factor, not a profitable entry rule. Episodic receipt lag and hypothetical fills limit actionability. | Replicate the unchanged risk result on a new covered window with participant and alternate-route checks. |
| K15 `KNOWN` | MC-006 independently matched 199,403/199,403 interior signatures. The unchanged top-flow risk direction repeated, but held-out top economic stress was negative under both fees. CreateEvent receipt-age p90 was 28.056 seconds despite 3,794 batch disk syncs. A second public websocket missed 466/871 independently indexed interior signatures in a short test. | Batch sync alone does not solve late receipt; neither tested public path establishes a uniformly timely, covered decision stream. The risk association remains uncertain and is not an entry rule. | Benchmark a faster stream with simultaneous clocks and independent coverage, then verify participant ownership and exit routes. |
| K16 `KNOWN` | MC-007 paired 11,831 common signatures across two public Solana websocket aliases; receipt times were almost identical and CreateEvent age p90 was about 1.5 seconds in this two-minute window. Both feeds included the same all-ones signature absent from independent index and later transaction queries, so both failed the frozen exact-coverage gate. | The alias does not establish an independent or sustained fast source, and the anomalous frame is not a verified launch. The short fast interval does not negate MC-005/MC-006 receipt bursts. | Dedicated independently indexed stream or Geyser benchmark with transaction-level notification truth and longer coverage. |
| K17 `KNOWN` | MC-008's full-transaction block subscription matched 7,481/7,481 independent interior signatures and 1,647 supported quotes, but CreateEvent age p90 was 10.872 seconds. Twenty-nine successful publicnode records carried `Log truncated`; an independent later official RPC fetch verified all 29 and recovered 14 omitted TradeEvents, including two new-mint state gaps. | Complete transaction signatures do not imply complete provider event logs. Later log repair cannot become early feature availability. This public block source is too late for the frozen early gate. | A faster dedicated creation/transaction stream; explicitly treat provider-truncated windows as unknown until later repair. |
| K18 `KNOWN` | MC-009 found PumpPortal 182 ms earlier at median on 44 matched creates, but absent for 14/58 native Pump core creates; seven other notifications were valid Bonk transactions. All 52 third-party signatures resolved to successful transactions. The official Pump log feed independently matched all 48,863 genuine interior signatures plus one uncorroborated all-ones frame. | The fast third-party feed is an incomplete, mixed-program selection of launches. A recurring malformed official notification must not become a scored token. Neither feed proves uniformly actionable early flow. | Freeze validation/quarantine of structurally invalid signatures on a new longer panel; separately benchmark an authenticated transaction stream if available. |
| K19 `KNOWN` | MC-010 prospectively matched 154,144/154,144 structurally valid interior signatures with zero truncated logs or protocol failures; all 310 create receipts met the coarse five-second age screen in that window. Its frozen top-flow groups had lower modeled curve-exit unavailability in both splits, but costed top stress means were negative at both fee scenarios. | One fast ten-minute panel does not erase earlier receipt bursts or establish positive realized returns. The unchanged net-flow entry rank failed its economic gates. | Route-aware exit and inclusion truth, actor independence, and another covered prospective window before any new feature test. |
| K20 `KNOWN` | MC-011 hash-selected 144 single-event early trades before later transaction retrieval. All 144 transaction signatures and slots matched; 142 had a unique event-sized token owner and all 142 matched emitted users. Two buys had ambiguous owner attribution, 44 transactions were routed at top level, and 47/144 whole-transaction fees exceeded 155,000 lamports. | This later owner check is not a five-second available feature or proof of independent beneficial control. It does not reverse MC-004's routed-sell mismatch or establish a profitable rank. | Preserve ambiguity, identify funding/control only where signed evidence supports it, and measure route-specific costs/exits on a new frozen panel. |
| K21 `KNOWN` | MC-012 later queried all 54 MC-010 curve-unavailable names and 54 quoted controls through four saved token-pair batches. Every unavailable name had only a `pumpfun` pair in the response; one control had a PumpSwap pair listed after its frozen 67-second exit. | Later third-party discovery does not prove historical route absence, route depth, size-aware cash recovery or actual liquidation. The MC-010 zero-recovery figure remains stress only. | Contemporaneous route quote and on-chain pool state at a frozen exit clock, with all failed or missing responses retained. |
| K22 `KNOWN` | MC-013 independently matched 208,728/208,728 valid interior signatures and saved 550/550 timely decoded Pump bonding-curve account reads for 275 creations. Later signed transactions confirmed 134/139 scored creations. Direct account and event-state stress returns agreed on 116/131 paired cases; the unchanged top-flow costed stress remained negative in both temporal splits under both fees. | Contemporaneous read-only accounts strengthen curve-state truth for this bounded window, but they do not show order inclusion, achieved fills, beneficial buyer independence or alternate-route liquidation. Earlier receipt-age bursts remain unresolved. | Freeze a different early risk/return feature and test it on a new temporal holdout with independent actor and route-aware execution evidence; benchmark a dedicated stream if available. |
| K23 `KNOWN` | MC-014 independently matched 48,270/48,270 valid interior signatures and retrieved all 135 selected first-three early-buy transactions. Every selected buy had a unique signed token owner by its creation's five-second checkpoint; all 61 native non-Mayhem core launches had complete selected-owner evidence. Owner, signer, fee payer and emitted user agreed in this selected sample, while eight buys were routed at top level. | This short-window selected result is not a continuous availability promise or beneficial-controller proof. All first-three owners within each launch were distinct, so simple distinct-owner count added nothing beyond capped buy count here; no economic outcome was tested. | Freeze a size/funding mechanism and test incremental costed value on a new covered panel, with explicit alternate-route and inclusion limitations. |
| K24 `KNOWN` | MC-015 matched 127,995/127,995 valid interior signatures and saved 532 timely direct Pump account reads plus 401 selected signed-buy responses. The frozen first-three owner-size dispersion score was known for 132/152 eligible launches. Its top direct-account costed stress returns were negative in both temporal splits and both fee scenarios, and worse than the unchanged flow top on the identical evaluation subset. | Reject this exact score as an entry basis under the tested curve quote/stress proxy. Do not infer realized zero recovery, independent beneficial buyers, actual Rocket inclusion or absence of other exit venues. | Route-aware exit state and inclusion/priority-fee evidence, plus a distinct prespecified mechanism on a new panel before another economic rank test. |
| K25 `KNOWN` | MC-016 found zero shared fee payers across different timely token owners in 123 eligible first-three-buy creations from MC-015. Owners signed 390/390 timely selected buys and equaled payer in 388/390; 43 payers recurred across creations. | Absence of a shared transaction payer does not prove independent funding or control. Cross-creation payer reuse alone does not imply collusion or quality. The rejected MC-015 economic rank stays rejected. | Signed prior funding-path retrieval with strict point-in-time limits if it can be justified; prioritize route and Rocket inclusion truth. |
| K26 `KNOWN` | MC-017 matched 107,302/107,302 independent interior signatures, saved 558 timely Pump curve reads and 169 timely canonical PumpSwap route reads. All 52 direct Pump curve-exit-unavailable names had a null canonical pool at their +67-second bank snapshot and no migration event visible by that response. Three canonical pools with decoded Token-2022 base and legacy WSOL quote vaults matched migration events, but their modeled entries were already unavailable. | The canonical migration route did not repair those 52 stressed exits at the measured snapshots. No other venue was completely indexed, and no hypothetical Rocket order or realized liquidation was observed. Positive virtual quote reserves in the present pools make raw quote vault balance an incomplete pricing state. | Preserve unknown noncanonical recovery; validate size-aware PumpSwap fees/rounding where a pool is present and measure hypothetical order inclusion/failure before another economic feature holdout. |
| K27 `KNOWN` | MC-018 queried 52 MC-017 depleted direct exits and 52 baseline-quoted controls through four saved DEX Screener batches 38–48 minutes after creation. No depleted exit had a listed noncanonical pool. A control's later PumpSwap listing matched its derived canonical pool and was timestamped after its frozen exit. | Later third-party listings cannot prove absence of a venue at the original clock or establish executable sell cash. The old generic `non-pumpfun` probe category includes canonical PumpSwap; the corrected assessment distinguishes it from a noncanonical pool lead. | Acquire an independently verified as-of noncanonical pool/vault state if one can be found; otherwise retain unknown recovery and prioritize route/inclusion calibration. |
| K28 `KNOWN` | MC-019's final official RPC response read Pump Fees FeeConfig, PumpSwap global config, three canonical pools, six vaults and three base mints from one confirmed bank. All three selected the first dynamic tier: 2 LP + 93 protocol + 30 creator bps, 125 total, while the static global fields were 20 LP + 5 protocol. The raw response and offline audit replay matched byte for byte. | Dynamic fee state and effective reserves are required for canonical sell pricing. This bank was later than MC-017's exit clocks and the three pools' modeled Pump entries were unavailable, so it does not repair any strategy outcome or prove executable cash. | Build and simulate an amount-specific sell with same-bank state and exact rounding; separately measure transaction landing/fill before another economic feature holdout. |
| K29 `KNOWN` | MC-020 hash-selected 36 successful signatures from the first 100-signature page on each of three canonical pools. All 36 signed transactions resolved after two explicit version-1 compatibility reads. Twenty-nine emitted pinned-layout selected-pool sells; all 29 reconciled effective-reserve constant-product gross, ceiling-rounded LP/protocol/creator fee amounts, event net identities and signed pre/post vault deltas. Fee schedules were 2/93/30 bps in 21 and 20/5/95 bps in eight. | This validates observed AMM sell arithmetic in a bounded landed sample. It does not establish an MC-017 as-of exit, a hypothetical Rocket simulation/fill, or independent user quote-cash delta; all 29 user quote accounts lacked comparable pre/post metadata. The original earliest-after-migration index gate failed and remains archived. | Construct a contemporaneous amount-specific unsigned sell and save read-only simulation; pursue separate landing and net-cash evidence. |

No validated memecoin edge or accepted strategy is recorded.
