# Memecoin experiment ledger

Append entries; do not overwrite a frozen design or result. Fields for every future entry: ID, parent, question, mechanism, falsifier, lifecycle timestamp/window, feature schema, label, protocol/fee versions, dataset fingerprint, execution model, metrics, temporal split, result, failure attribution, status, allowed/forbidden conclusion, next implication, git SHA.

## MC-001 — point-in-time early panel gate

| Field | Frozen value |
| --- | --- |
| Parent | Historical #27/#30 audit; no inherited threshold |
| Question | Can Rocket observe a covered early-launch universe with features actually available at decision time? |
| Mechanism | Raw create/trade receipts plus ordered on-chain state provide early features without future leakage. |
| Cheap falsifier | Missing creation coverage, unknown receive delay, unsupported account versions or unavailable source provenance. |
| Lifecycle window | To be frozen before a prospective collection session; creation through pre-graduation. No retrospective fixture is silently treated as live. |
| Feature/label | `rocket.memecoin.pit-snapshot.v1`; no outcome label joined yet. Later label: size-aware realizable return and separate protocol survival/risk. |
| Protocol / fee | Record per event; unknown remains unknown. |
| Dataset fingerprint | None: the prospective cohort has not been captured. Individual rows will be hashed by the snapshot builder. |
| Execution | Disabled. Later simulator must pin protocol fee, size, latency, slippage, failure and exit rules. |
| Baseline / metrics / split | Age/curve-only baseline; coverage and latency first, then temporal OOS ranking and net expectancy. Split cannot be frozen until a collection window exists. |
| Result / failure attribution | `KNOWN` for the bounded MC-001 window: 2,458/2,458 inner-slot signatures matched; six creates/437 trades decoded; 113/113 native non-Mayhem state transitions and 208/208 observed quotes reconciled. Initial 8 MiB capture failed its disk bound; the completed session used 32 MiB. See [result](experiments/MC-001-RESULT.md). |
| Allowed conclusion | A read-only prospective early-event stream can be captured and audited for a short window; the data contract rejects late or unprovenanced features. |
| Forbidden conclusion | Any early predictive signal, graduation alpha, trade expectancy or `ENTER` decision. |
| Next implication | Run the predeclared longer MC-002 collection and quote baseline; test regime coverage and execution beyond the short calibration. |
| Git SHA | To be filled by PR commit; design created on 2026-09-24. |

## MC-002 — five-second curve-progress / costed quote proxy

Parent MC-001. Status `OPEN` / no promotion. [Frozen question, mechanism, falsifier, population, five-second features, 60-second label approximation, 70/30 temporal split, costs and forbidden conclusion](experiments/MC-002-FROZEN.md). [Result and evidence](experiments/MC-002-RESULT.md): capture-manifest SHA-256 `eecafe4a98590184e8d88963cd195532480bfd80c394f9fc3a26658923e429da`; 104 creates, 41 scored, 19 quoted, 22 censored. Evaluation had three numeric quote outcomes. Failure attribution: `DATA_LIMITATION` (small numeric and temporally held-out sample), `EXECUTION_UNREPRODUCIBLE` (static-state proxy and unavailable exits), not `NO_SIGNAL`. Allowed conclusion: the current simple curve-progress rank is not promoted. Forbidden conclusion: a validated profitable strategy or proof that all early features lack signal. The original misclassified-exit result and corrected result are both in the archive. No frozen clock, size, cost or rank was retuned.

## MC-003 — exit feasibility and replication

Parent MC-002. Status `REJECTED` as an entry basis. [Frozen design](experiments/MC-003-FROZEN.md) and [result](experiments/MC-003-RESULT.md). A new 300-second cohort yielded 102 creates, 52 scored names, 40 quoted exits and 12 exit-unavailable names. The unchanged rank's held-out all-quoted mean was −8.99% (11); top-quartile mean −8.05% (3). The zero-recovery stress mean was −33.67% for all 15 and −31.42% for the top four. Failure attribution: `NO_SIGNAL` for this simple rank under the frozen quote proxy; `EXECUTION_UNREPRODUCIBLE` for actual fills and zero-recovery valuation; `DATA_LIMITATION` for cohort duration and held-out size. No model is promoted. Raw and second-provider index evidence are archived; implementation commit `0f81aae`.

## MC-004 — transaction accounting calibration

Parent MC-003. Status `ACCEPTED` for bounded protocol/cost calibration, not for strategy entry. [Frozen selection](experiments/MC-004-FROZEN.md) and [result](experiments/MC-004-RESULT.md). All 56 selected transactions were retrieved and matched stream signature/slot. One transport version amendment was recorded before analysis without changing the sample. Forty-seven of 48 single-event token transfers reconciled to the event amount; the remaining create-and-buy lacked a pre-balance. Fifteen of 48 single-event network fees exceeded the older 155,000-lamport scenario. Failure attribution: `EXECUTION_UNREPRODUCIBLE` remains for hypothetical Rocket inclusion and exit fills; not a signal test. Raw responses and deterministic replay are archived.

## MC-005 — early net flow on an independent covered panel

Parent MC-003/MC-004. Status `CANDIDATE` for the exit-risk association, `REJECTED` as an entry rule. [Frozen design](experiments/MC-005-FROZEN.md) and [result](experiments/MC-005-RESULT.md). A fresh 600-second cohort matched 152,120/152,120 second-provider interior signatures; 141 launches were scored. The top flow quartile had 1/25 development and 0/11 evaluation unavailable curve exits, versus 19/57 and 14/31 among other entry-known names. All predeclared held-out minimum gates passed, but development stress returns were negative and one +288.65% modeled winner drove the positive held-out mean; leave-one-out held-out top means were negative under both fee scenarios. Failure attribution: `COST_DRAG`, `DATA_LIMITATION`/outlier sensitivity, `EXECUTION_UNREPRODUCIBLE` for hypothetical orders, and episodic receipt latency. Allowed: independent risk replication. Forbidden: an accepted `ENTER` rule or economic edge claim. Archive and offline replay preserve the trial; implementation commit `40a3996`.

## MC-006 — unchanged flow-risk replication and latency gate

Parent MC-005. Status `CANDIDATE` for directional exit-risk association; `REJECTED` for fast-discovery and economic entry. [Frozen design](experiments/MC-006-FROZEN.md) and [result](experiments/MC-006-RESULT.md). A 600-second v2 capture matched 199,403/199,403 second-provider interior signatures; 147 launches were scored. Unchanged top-flow groups had lower unavailable curve exits than the rest in development (4/26 versus 22/75) and evaluation (0/11 versus 6/32), with wide exact uncertainty. Both costed economic top means were negative in both splits. CreateEvent receipt-age p90 was 28.056 seconds, failing the prespecified five-second gate despite a batch-synced collector. Alternate public websocket evidence failed independent signature coverage. Failure attribution: `DATA_LIMITATION` for high receipt age and small risk counts, `COST_DRAG` for negative proxy economics, `EXECUTION_UNREPRODUCIBLE` for hypothetical fills and alternate exits. Allowed: target a faster independently covered stream and verify actors/routes. Forbidden: an accepted profitable or actionable entry rule. Archive and replay preserve raw data; implementation commit `775398e`.

## MC-007 — simultaneous public websocket acquisition benchmark

Parent MC-006. Status `REJECTED` for both public aliases under the frozen exact-coverage gate. [Frozen design](experiments/MC-007-FROZEN.md) and [result](experiments/MC-007-RESULT.md). Two simultaneous 120-second captures had 11,831/11,831 common signatures in overlapping interior slots and mainnet-minus-beta paired receipt p99 of 108 milliseconds. CreateEvent receipt-age p90 was about 1.5 seconds for both. Each arm also emitted the same all-ones signature in one uncorroborated frame, absent from the independent index and later transaction-status query. Thus both arms failed exact coverage despite no collector errors. Failure attribution: `DATA_INTEGRITY` for the unexplained notification, `DATA_LIMITATION` for the two-minute observation horizon. Allowed: investigate a dedicated independently indexed stream. Forbidden: waiving the frozen gate or claiming the alias fixes episodic receipt delays. Raw frames and both indexes are archived; implementation commit `775398e`.

## MC-008 — full-transaction block stream feasibility

Parent MC-007. Status `REJECTED` as fast complete early-event acquisition. [Frozen design](experiments/MC-008-FROZEN.md) and [result](experiments/MC-008-RESULT.md). A 60-second, bounded publicnode `blockSubscribe` session stored 228 blocks and 7,553 transactions. Official independent indexing matched 7,481/7,481 interior signatures; native quotes passed 1,647/1,647. CreateEvent receipt-age p90 was 10.872 seconds, failing the five-second gate. Twenty-nine successful transactions had truncated logs, causing two new-mint reserve-continuity failures despite complete signatures. Failure attribution: `LATENCY` and `LIFECYCLE_LOG_INCOMPLETE`; transaction identities were recoverable from signed bytes, but strict create-instruction identity remains unverified. Allowed: use the block source for transaction audit/backfill. Forbidden: economic scoring or declaring full early-event coverage from signature coverage alone. Raw, derived and index evidence are archived; implementation commit `bc46d64`.

## MC-009 — paired third-party creation feed

Parent MC-008. Status `REJECTED` as a complete Pump discovery source. [Frozen design](experiments/MC-009-FROZEN.md) and [result](experiments/MC-009-RESULT.md). Two simultaneous 180-second read-only captures found the third-party feed about 182 ms earlier at median on 44 matched creates, but it missed 18/62 chain core CreateEvents, including 14/58 native and 13/43 strict native non-Mayhem launches. Its seven unmatched Pump-program events were valid Bonk transactions outside the target program. The official log arm had 48,863/48,863 independently indexed interior signatures plus one recurring uncorroborated all-ones signature, failing the original exact raw-feed gate. Failure attribution: `DISCOVERY_COVERAGE` and `SOURCE_SCOPE`, with `DATA_INTEGRITY` for the official feed artifact. Allowed: investigate a predeclared signed-transaction quarantine rule and dedicated stream. Forbidden: treating this third-party feed as the full Pump universe or tuning an entry rule on its observed subset. Archive and offline replay preserve both feeds and 52 transaction responses; implementation commit `bc46d64`.

## MC-010 — fast-receipt signed flow cohort

Parent MC-005/MC-006/MC-009. Status `ACCEPTED` for its bounded acquisition contract and repeated directional exit-risk component, `REJECTED` as an economic entry rule. [Frozen design](experiments/MC-010-FROZEN.md) and [result](experiments/MC-010-RESULT.md). A prospective 600-second panel matched 154,144/154,144 valid interior signatures with zero quarantined frames, truncated successful logs or protocol failures. All 310 creates arrived within the five-second coarse timestamp screen; 240 were analytically eligible and 238 had strict signed-create identity. The unchanged top-flow rank had fewer unavailable curve exits in both development (7/42 versus 35/88) and evaluation (1/18 versus 11/33). Costed top stress means were negative in both splits and both fee scenarios; held-out leave-best-out means were negative. Failure attribution: `NO_ECONOMIC_SIGNAL` for this unchanged proxy, `EXECUTION_UNREPRODUCIBLE` for actual inclusion/alternate exits, and `IDENTITY_SCOPE` for two unresolved compound/routed creates. Allowed: targeted buyer-identity, alternate-exit and dedicated-feed tests. Forbidden: `ENTER`, a net-positive strategy claim, or using this cohort to retune flow. Archive SHA-256 `704a48905b3dd0fb993f7dd44775d5642faa5da4a6cdf647f8e5199c59c924e7`; offline replay matched ten derived hashes. Frozen design and implementation commit `bc46d64`.

## MC-011 — signed early-trade actor/route calibration

Parent MC-004/MC-010. Status `ACCEPTED` for bounded single-event account semantics, `OPEN` for beneficial-control and economic feature value. [Frozen design](experiments/MC-011-FROZEN.md), [result](experiments/MC-011-RESULT.md), and selection commit `aac0693` preceded independent transaction retrieval. Hash-stratified sampling took 144 early single-event signatures from 2,157 eligible trades across top/rest and buy/sell groups. All 144 transactions resolved and matched observed signature/slot. An event-sized token-account owner was identifiable in 142; all 142 matched the emitted user, while two buys lacked a matching positive account delta and stayed ambiguous. Forty-four transactions entered Pump through a different top-level program, and 47/144 whole-transaction fees exceeded 155,000 lamports. Failure attribution: `BENEFICIAL_CONTROL_UNKNOWN` and `POINT_IN_TIME_IDENTITY_UNAVAILABLE`; this later account check cannot become an early feature. Allowed: use the owner-match interval and route mix to design later funding/cashflow tests. Forbidden: assuming every event user is a distinct beneficial buyer, treating observed payer fees as Rocket fill costs, or changing MC-010's rank/return conclusion. Evidence archive SHA-256 `edd82a6033411784383b223913076fc046c1dd18fede758bc07d76f9cffd6c06`; extracted replay matched the report byte for byte.

## MC-012 — later alternate-pool discovery check

Parent MC-010. Status `ACCEPTED` as bounded **later discovery** evidence, `OPEN` for contemporaneous alternate-route execution. [Frozen design](experiments/MC-012-FROZEN.md), [result](experiments/MC-012-RESULT.md), and selection commit `38181c3` preceded four saved no-key API requests. All 54 MC-010 unavailable-curve names had only a `pumpfun` pair in the later response; among 54 quoted controls, one PumpSwap pair was listed after its frozen exit clock. No third-party pair here supplies a 67-second size-aware quote or proves an absent venue. Failure attribution: `AS_OF_ROUTE_STATE_UNAVAILABLE`; later discovery cannot resolve realized liquidation. Allowed: prioritize contemporaneous route/pool acquisition. Forbidden: relabeling MC-010 stress returns or interpreting no later listing as proof of no alternate exit. Archive SHA-256 `a282cd5aa83fdc6d9e17b6621bd58ba84ca22f67f1874dc09463f7f91c04c6b2`; extracted replay matched the report.

## MC-013 — direct curve-account quote calibration

Parent MC-010/MC-012. Status `ACCEPTED` for a bounded contemporaneous Pump account acquisition and quote comparison, `REJECTED` for the unchanged flow economic entry rule. [Frozen design and pre-cohort transport amendment](experiments/MC-013-FROZEN.md), [result](experiments/MC-013-RESULT.md). The first two failed Node attempts remain in a separate archive; the usable Python 600-second panel matched 208,728/208,728 valid interior signatures, with zero log/protocol failures. The companion saved 550/550 timely decodable account responses for all 275 creates. Later signed transactions strictly verified 134/139 scored creates. Direct-account and event-state stress returns agreed exactly for 116/131 paired outcomes; 15 differed, without repairing negative top-flow stress means in either split under either fee scenario. Failure attribution: `NO_ECONOMIC_SIGNAL` under the tested proxy, `EXECUTION_UNREPRODUCIBLE` for actual fills/alternate exits, `BENEFICIAL_CONTROL_UNKNOWN`, and `ACQUISITION_RELIABILITY_UNPROVEN` beyond the bounded window. Allowed: use direct account clocks/slots to calibrate a fresh route-aware hypothesis. Forbidden: `ENTER`, inferring realized zero recovery, or retuning flow on this cohort. Archive SHA-256 `f187c704021946e4bc5453e69d0d80f76f08f11e868b1ef7bf3cd518a40f41b7`.

## MC-014 — signed early-buyer decision-clock availability

Parent MC-011/MC-013. Status `ACCEPTED` for short-window read-only signed token-owner availability, `OPEN` for beneficial control and economic value. [Frozen design](experiments/MC-014-FROZEN.md), [result](experiments/MC-014-RESULT.md), design/implementation commit `d024cb2` before the 180-second capture. The primary stream matched 48,270/48,270 valid interior signatures with zero log/protocol failures. All 135 selected first-three early-buy transactions had a unique signed token-account owner by their creation's five-second checkpoint; all 61 native non-Mayhem covered launches had all selected owners available. Owner matched emitted user, signer and fee payer in this selected sample; eight transactions were routed at top level. First-three owners were distinct in every launch, so their count merely reproduces capped buy count here. Failure attribution: `BENEFICIAL_CONTROL_UNKNOWN`, `ECONOMIC_VALUE_UNTESTED`, `LONG_WINDOW_RELIABILITY_UNPROVEN`. Allowed: freeze a new signed buyer-size/concentration feature and test on a separate costed holdout. Forbidden: treating distinct addresses as independent funding, transferring this 100% rate to all trades/windows, or `ENTER`. Archive SHA-256 `c7bb3e6bd726b9597616797d9783751b3a1c175a134251f77b9deafe1cd8ac0b`; extracted replay matched three derived hashes.

## MC-015 — signed first-three buyer-size dispersion

Parent MC-013/MC-014. Status `REJECTED` as an economic entry rank under the frozen direct Pump curve quote/stress model. [Frozen design](experiments/MC-015-FROZEN.md), [result](experiments/MC-015-RESULT.md), design/analysis commit `438c77f` before the 600-second capture. The independent index matched 127,995/127,995 valid interior signatures with zero log/protocol failures. Both companions parsed the entire raw segment: 532/532 timely decoded account reads and 401/401 selected signed-buy responses. The normalized size-dispersion score was known for 132/152 eligible launches after the timely-owner and strict-create screens. The top group had negative costed direct-account stress means in development and evaluation under both fee scenarios; the evaluation top was worse than flow on the same score-known subset. Minimum sample gates passed; economic gates failed. Failure attribution: `NO_ECONOMIC_SIGNAL` for this score/proxy, `EXECUTION_UNREPRODUCIBLE`, `BENEFICIAL_CONTROL_UNKNOWN`, `LONG_WINDOW_RELIABILITY_UNPROVEN`. Allowed: focus on verified route/inclusion and funding truth. Forbidden: threshold retuning on MC-015, realized zero-recovery claims or `ENTER`. Archive SHA-256 `0a2ac1f6745271b3a00dad7cd331bb05f86588f3e41efb590de85553ddf47e8e`; extracted offline replay matched all 11 derived-file hashes, including dispersion result SHA-256 `8f3fa81b347f252af569f9e311ef1ec2bb63bcbfebac6f68ed48cd61a443700d`.

## MC-016 — signed buyer payer overlap diagnostic

Parent MC-015. Status `ACCEPTED` for a bounded negative payer-overlap observation; common funding remains `OPEN`. [Frozen design](experiments/MC-016-FROZEN.md), [result](experiments/MC-016-RESULT.md), design commit `9433451` before payer-overlap analysis. All 401 selected core buys stayed in the denominator; 390 had timely signed token-owner truth. Among 123 creations with two or more timely distinct owners, zero had a shared fee payer across different owners. Owners signed 390/390 timely transactions and equaled fee payer in 388/390. Forty-three payers recurred across creations. Failure attribution: `BENEFICIAL_CONTROL_UNKNOWN` for the broader funding question; the frozen shared-payer observation itself was absent. Allowed: report that this narrow common-payer form was not observed. Forbidden: inferring independent beneficial funding, retuning MC-015, or changing its economic decision. Deterministic report SHA-256 `3ff113d0672ebc17a2eaeb36c1321cec69490aa025ab9adc6d7d6d1baf5b29f9`; extracted MC-015 replay reproduced it byte for byte.

## MC-017 — canonical PumpSwap route state near the exit clock

Parent MC-012/MC-013/MC-015. Status `ACCEPTED` for a bounded canonical-pool absence/presence observation, `OPEN` for other venues and achieved execution. [Frozen design](experiments/MC-017-FROZEN.md), [result](experiments/MC-017-RESULT.md), design commit `def98bc` and route capture commit `d639613` before the 600-second cohort. The independent index matched 107,302/107,302 valid interior signatures with no log/protocol failures. All 558 Pump account reads and 169 canonical route first reads met frozen timing bounds. Of 154 baseline-scored names, 52 had an unavailable Pump curve sell from insufficient real quote liquidity; all 52 had a null canonical PumpSwap pool in the timely same-bank route read, with no same-mint migration event visible by that response. Three present canonical pools had fully decoded vaults and matching migration events, but their modeled Pump entries were already unavailable. Failure attribution: `EXECUTION_UNREPRODUCIBLE` for hypothetical Rocket orders, `OTHER_ROUTE_STATE_UNKNOWN` for noncanonical liquidation, `LONG_WINDOW_RELIABILITY_UNPROVEN` beyond bounded panels. Allowed: reject a canonical PumpSwap migration repair at the measured exit snapshots. Forbidden: declaring realized zero recovery, no alternative venue, a validated edge or `ENTER`. Archive SHA-256 `592fe7e5fbbb8f70f4b448b0ec35e19f1f35d641930c3ef49a2704d1683d9063`; extracted replay matched ten derived files including route report SHA-256 `b9908c92251575049a9451f2a8235724edbe62fc8ca87ebbbfefd618cacff4b2`.

## MC-018 — later alternate-pool listing triage

Parent MC-017/MC-012. Status `ACCEPTED` as an exploratory later-discovery observation, `OPEN` for as-of noncanonical venue truth. [Result](experiments/MC-018-RESULT.md). The deterministic MC-012 selection rule chose all 52 MC-017 baseline curve-unavailable mints and 52 quoted controls; all 52 unavailable mints matched stricter direct account `EXIT_UNAVAILABLE`. Four successful API batches, queried 38–48 minutes after creation, listed a Pump curve but no noncanonical pool for any of those 52. One control's later `pumpswap` listing was its independently derived canonical pool and arose after its frozen exit; the generic old probe's “other venue” category is corrected in a separate deterministic assessment. Failure attribution: `AS_OF_ROUTE_STATE_UNAVAILABLE` and incomplete third-party venue coverage. Allowed: prioritize independently verified contemporaneous route state over this listing source. Forbidden: declaring no alternate venue, a zero realized recovery, or an economic edge. Archive SHA-256 `1745d076619881cd76c14882a65f1f0dc4cdaf84b8a8c384644d779647b5c196`; assessment SHA-256 `321067cab37a434ecc591a4b56251ca53359b149dbaee7a251fdaac395cb021c`.
