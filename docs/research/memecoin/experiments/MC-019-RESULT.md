# MC-019 result — dynamic PumpSwap fee state in one later confirmed bank

Status: **accepted as bounded route-pricing input calibration**, not an as-of MC-017 exit quote or achieved execution. The [design](MC-019-FROZEN.md) was committed at `88078fd` and the fixed-account capture code at `87ae753` before the RPC reads. A byte-preserving capture amendment was committed at `12c39e0` before the final raw response. The selected three mints were exactly MC-017's three already present canonical pools; none represented a successful modeled Pump entry.

The initial publicnode 14-account `getMultipleAccounts` request at 21:28:31 UTC returned HTTP 403 with RPC error `Request blocked`. Solana's official public RPC accepted the identical frozen address list twice. The final byte-preserving confirmed read dispatched at **2026-09-24 21:29:19.792296 UTC**, returned at **21:29:20.369917 UTC** and supplied one context slot, **450150569**, for the Pump Fees `FeeConfig`, PumpSwap global config, three pools, six vaults and three Token-2022 base mints. Exact response bytes, their SHA-256, request, clocks and both earlier attempts are archived. The offline audit verified the response bytes, selection, all account owners/discriminators, pool and vault identities, mint supplies, Token-2022 extension types and positive reserves. The data gate passed.

The Pump Fees account contained 25 strictly increasing regular tiers. All three pool market caps, computed with **effective** quote reserves (raw WSOL vault plus the observed nonzero pool virtual reserve), were below the second tier's 420 SOL threshold. They therefore selected the first tier: **2 bps LP + 93 bps protocol + 30 bps creator = 125 bps total**. The PumpSwap global config's static fields were **20 bps LP + 5 bps protocol**; using those alone would omit the active dynamic tier. The selection follows the pinned [official market-cap and fee-tier rule](https://github.com/pump-fun/pump-public-docs/blob/81091419e4457566469d4e2a27f64ed84d42419c/docs/FEE_PROGRAM_README.md) and the [effective-reserve definition](https://github.com/pump-fun/pump-public-docs/blob/81091419e4457566469d4e2a27f64ed84d42419c/docs/PUMP_SWAP_README.md). The docs' descriptive claim that virtual reserves are zero does not match these decoded accounts.

| Mint prefix | Effective-reserve market cap | Selected threshold | LP / protocol / creator bps |
| --- | ---: | ---: | ---: |
| `9Nnvxfav…` | 402.935 SOL | 0 SOL | 2 / 93 / 30 |
| `DCwRzNLG…` | 17.873 SOL | 0 SOL | 2 / 93 / 30 |
| `FbJMNyaX…` | 17.873 SOL | 0 SOL | 2 / 93 / 30 |

Each base mint had observed Token-2022 extension type IDs 18 and 19; each base vault had ID 7. The audit rejects any other extension type, including a transfer-fee extension, rather than assuming it is economically inert. This is an input gate only. Exact size-aware sell rounding, fee amount rounding, instruction account construction, read-only simulation, transaction landing, market response and realized net cash remain unmeasured. The 21:29 bank is about 2 hours 36 minutes after the MC-017 cohort and cannot be backdated to its +67-second exits. The 52 depleted exits still lacked a canonical pool at their own saved bank snapshots; their noncanonical recovery stays unknown.

The [evidence archive](../data/mc019-amm-fee-state-20260924.tar.xz) has SHA-256 `c600e270f164d0fd852b7a7ee0987db90bd09a3866998fb25ea34e8e223a4e34` (69 KiB). Extracted offline replay reproduced the audit SHA-256 `e0264ce2017324c6b49000aed961dabe86e9f3d825b1e31a10446def006b6144` byte for byte. This accepted calibration makes the next executable-route study more concrete: pair same-bank dynamic fees and effective reserves with an amount-specific sell instruction and read-only simulation, then test actual inclusion separately. It does not promote any entry rank or `ENTER` decision.
