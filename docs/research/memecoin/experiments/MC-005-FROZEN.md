# MC-005 frozen design — early flow and exit risk on a fresh panel

Parent MC-003/MC-004. Freeze time: before the new session begins. The five-second curve-progress entry rank failed MC-003; do not tune it here. Acquire one new 600-second, at most 512 MiB, read-only Pump `logsSubscribe` session. Reconcile fully observed interior-slot signatures against a different RPC provider; require zero Pump decode errors and zero supported native non-Mayhem state/quote failures. Preserve every create, rejected regime, missing observation, raw frame and source clock. The later transaction index and account reads are validation evidence, not point-in-time early features.

**Question and mechanism:** does immediately observed net buying pressure identify launches that remain exit-feasible and offer better costed returns than the full early-launch universe? The proposed mechanism is genuine early participant demand, but bot churn, coordinated wallets and launch-time price impact could make this count misleading. A cheap falsifier is a nonpositive top-quartile held-out stress return or no improvement in exit feasibility; either result rejects this simple count as an entry basis under the tested proxy. It would not prove all flow or participant features useless.

Use the same native-SOL/non-Mayhem population, t0+5s feature clock, 0.01 SOL size, t0+7s entry and t0+67s exit. Maintain 2% adverse slippage each leg and the static-state quote proxy. Define the **sole new rank** as `observed_buys_5s - observed_sells_5s`, counted from Pump TradeEvents received by t0+5s. Sort descending, breaking ties by create signature. This is a simple observable order-flow activity hypothesis, not wallet quality or manipulation proof. Do not search alternative windows, scores or cutoffs in this cohort.

Chronologically split eligible scored creates 70% development / 30% untouched evaluation. Primary risk outcome: an as-of entry is quoteable but the t0+67s state cannot sell the stressed token inventory (`EXIT_UNAVAILABLE`). Missing entry/coverage remains `UNKNOWN`, never a negative risk case. Primary economic outcome: static-state net quote proxy with unavailable exits charged at zero recovery. Report all scored versus top quartile in both splits. Keep the inherited 155,000-lamport per-leg network scenario and add a predeclared **1,000,000-lamport per-leg** adverse fee scenario informed by MC-004's sampled high-fee tail; do not substitute that sample's observed fees for a hypothetical Rocket order. Report quoted-only returns separately for comparability.

Promotion requires a covered, protocol-valid fresh panel, at least 30 evaluation scored launches with known entry state, lower exit-unavailability rate for the top quartile than for all evaluation launches, and positive top-quartile net stress mean under **both** fee scenarios with improvement over all evaluation launches. These are minimum gates, not statistical proof; uncertainty and actual inclusion/route feasibility still need separate evidence. If a gate fails or the sample is too small, do not promote. Preserve the exact result and next falsifier without retuning this panel.

Documentation clarification added during collection, before audit or result: the evaluation slice is held untouched until the full capture and audit finish. Report cohort exclusions, missing entry states, tie frequency, quoted-only count and mean, risk rate and zero-recovery stress mean/median by split and rank group. Classify failure as `NO_SIGNAL`, `COST_DRAG`, `DATA_LIMITATION`, `EXECUTION_UNREPRODUCIBLE` or protocol/label error only as supported by evidence. No score, clock, cost, population or promotion gate was changed by this clarification. No frozen design conclusion may be rewritten after seeing the result.
