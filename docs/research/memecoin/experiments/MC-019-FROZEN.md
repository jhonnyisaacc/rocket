# MC-019 frozen design — canonical PumpSwap fee-state calibration

Parent MC-017/MC-018. Question: can the actual canonical PumpSwap fee configuration, mint supply, pool and vault reserves be obtained from a **single confirmed bank** for the three pools MC-017 already found present? This is a route-pricing input calibration, not an early-entry or profit test.

Selection: all and only the three MC-017 rows with `CANONICAL_POOL_VAULTS_DECODED`, as fixed by the archived MC-017 route report. Preserve their mints, canonical pool PDAs and vault addresses. Derive the Pump Fees `fee_config` PDA from the pinned PumpSwap IDL seeds `[b"fee_config", PumpSwap program id]` under fee program `pfeeUxB6jkeY1Hxd7CsFCAjcbHA9rWtchMGdZ6VojVZ`; require it to equal `5PHirr8joyTMp9JMm6nW7hNDVyEYdkzDqazxPD7RaTjx`. Include that PDA, the PumpSwap global config, all three pools, both vaults per pool, and each base mint in one `getMultipleAccounts` confirmed request. Save exact request/receive clocks, request addresses, HTTP status, raw response SHA-256, body and bank slot. No transaction is signed or sent.

Audit gates: exactly one response for the frozen address list; valid account owners/discriminators and mint/pool/vault identities; one bank context; decodable Pump Fees `FeeConfig` tiers, PumpSwap global config, pool virtual quote reserves, Token-2022 mint supply, and base/quote vault amounts. Require positive base reserve, effective quote reserve and mint supply. Compute `market_cap_lamports = floor(effective_quote_reserve * mint_supply / base_vault_amount)` and choose the highest fee tier whose threshold does not exceed that cap, per the pinned [official fee specification](https://github.com/pump-fun/pump-public-docs/blob/81091419e4457566469d4e2a27f64ed84d42419c/docs/FEE_PROGRAM_README.md). Record the tier threshold and LP/protocol/creator bps; do not silently substitute the global static bps. Decode unsupported extensions as an explicit failure, not a guessed quote.

The observed bank and receipt clock are the only as-of time for this calibration. The three MC-017 modeled Pump entries were already unavailable before their exit clocks, so this measurement cannot repair their strategy outcomes or MC-017's 52 depleted exits. A positive fee-state gate still does not establish exact sell rounding, transaction construction, simulation success, landing or realized cash. Those remain separate Frontier steps. Preserve raw data and replay the deterministic audit before interpreting the result.
