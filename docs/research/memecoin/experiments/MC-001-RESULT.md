# MC-001 result — bounded early-event capture gate

Status: `KNOWN` for this bounded session, 2026-09-24. This is decoder/coverage calibration, not a trade experiment.

The first 20-second trial hit its 8 MiB segment bound after 12.8 seconds, proving the initial cap was too small. A completed 10-second trial captured 2,731 Pump log notifications plus subscription and transaction responses. The raw segment and five independent `getSignaturesForAddress` pages are retained in the compressed evidence archive. Official Pump IDL revision `81091419e4457566469d4e2a27f64ed84d42419c`, SHA-256 `ffe966c42f1af41652ee753fe2f1e3f7cd4077d7e6f49faf3138959c8b56064b`, was pinned before decoding.

Evidence: [`mc001-capture-20260924.tar.gz`](../data/mc001-capture-20260924.tar.gz) (archive SHA-256 `f80bb22ffc6de00836dc2f926757dfca16bcd99f51a6bc233b9afcae3476c3b0`); initial disk-bound [manifest](../data/mc001-bounded-failure-manifest.json). To reproduce offline, extract the archive into a new directory and run `python scripts/research/memecoin_audit.py DIRECTORY --offline --max-pages 12`. The replay reproduced the signature counts, decoded-event counts, state-continuity checks and identity comparison.

In fully observed slots 450057564–450057595, **2,458/2,458 signatures matched** the separate RPC index, with zero missing in either direction. First and last observed slots were excluded because subscriptions can start/stop mid-slot. This supports coverage only for the interior slots of this session. The median RPC whole-second block timestamp to measured websocket receipt was 1.427 seconds (p90 1.865; range 0.702–2.204); this is a coarse event-age measure, not exact network latency.

The pinned IDL strictly decoded six `CreateEvent` and 437 `TradeEvent` records with zero event-layout errors. Five create mints matched the signed create-v2 transaction's first account; one transaction response was unavailable at capture time, so that second-source identity check remains open. The on-chain CreateEvent bytes themselves carried its mint at the recorded websocket receipt. For newly created native-SOL, non-Mayhem tokens, **113/113** adjacent trade-state transitions reconciled in chain index order; eight Mayhem transitions were excluded from that formula. In the same captured period, the current integer quote/fee implementation reproduced 208/208 observed native non-Mayhem buy/sell events across four instruction families. This verifies the tested arithmetic regime, not fills for a new trader or every Pump version.

The event stream is now a viable *bounded* prospective early observation source. Its 10-second cohort is too small and too short to test a 60-second economic outcome. MC-002's feature and outcome definitions were frozen before a longer cohort is captured. No model or Rocket entry decision is promoted.
